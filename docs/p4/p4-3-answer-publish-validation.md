# P4-3 回答发布编排验证

验证日期：2026-08-20

## 交付范围

`AnswerPublishController` 覆盖新建与既有回答回填、本地草稿→远端草稿→正式发布的固定顺序、双层成功 ID 解析，以及 `103003` 重新查询后转编辑。401、403、429 和一般失败均保留本地草稿；离页取消通过 generation 门禁拒绝迟到回写。

## 自动化证据

- `AnswerPublishState.test.ets` 以 fake 仓储覆盖新建、编辑、成功响应、冲突、错误保留、访客门禁和取消迟到响应；
- 主线集成 Hypium `457/457` 全通过；完整 HAP 构建成功，编译 API 26、target/compatible API 24。

## 已知限制与设备验收

当前交付为可注入编排状态机，尚未自动发送真实发布请求。后续页面接线须在 API 24 专用测试账号上手动完成文本回答、带图回答、编辑和重启恢复，并在最终确认后才改变外部状态。
