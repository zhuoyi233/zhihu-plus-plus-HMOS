# P4-4 想法发布状态机验证

验证日期：2026-08-20

## 交付范围

`PinPublishState` 固定本地草稿→远端草稿→发布顺序，处理发布成功 ID 与本地状态；失败和 401/403/429 保留内容。状态机同时限制标题、正文、图片与去重话题，计算 HTML 文本长度，并为话题检索提供防抖、取消、迟到响应和固定失败回退。

## 自动化证据

- `PinPublishState.test.ets` 使用 fake 覆盖发布顺序、成功、错误保留、话题去重、防抖、取消和回退；
- 主线集成 Hypium `457/457` 全通过；完整 HAP 构建成功，编译 API 26、target/compatible API 24。

## 已知限制与设备验收

当前为页面可注入状态机，不会自行创建远端想法。后续页面接线须用专用测试账号在 API 24 设备手动验收文本、图片、话题和强制停止恢复，且不得把请求体、Cookie 或草稿正文写入日志。
