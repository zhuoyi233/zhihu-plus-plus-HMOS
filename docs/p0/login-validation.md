# P0 登录原型验证记录

> 首次验证：2026-07-20；真实账号闭环：2026-08-11

## 已实现路径

- 游客模式不发起账号请求，也不会隐式删除已经加密保存的登录会话。
- 手动 Cookie 导入使用密码输入框，UI 和测试树只暴露掩码，不显示 Cookie 明文。
- 输入在提交后立即从页面状态清空。
- 登录 Cookie 必须同时包含 `z_c0` 和 `d_c0`；缺失时在本地拒绝，不发起网络请求。
- 合法格式的 Cookie 会携带桌面 User-Agent、`Referer`、`x-requested-with`、`x-zse-93` 和动态生成的 `x-zse-96` 请求 `https://www.zhihu.com/api/v4/me`。
- 只有 HTTP 200 且账号响应包含非空 `id`、`name` 时，Cookie 才会进入加密会话存储。
- HTTP 401/403 视为凭证失效；恢复旧会话时会清理本地密文和 Asset Store 密钥。
- 网络错误、服务端错误和无法识别的响应不会删除已保存会话，避免弱网导致误登出。
- 退出登录会同时清除 Cookie 密文和 Asset Store 数据密钥。

## 自动化测试

新增 6 个 ArkTS 用例，覆盖：

1. `z_c0` 与 `d_c0` 的组合要求。
2. `/api/v4/me` 账号响应解析。
3. 缺字段和非法 JSON 拒绝。
4. HTTP 200 登录成功分类。
5. HTTP 401/403 凭证失效与 503 临时失败分类。
6. HTTP 200 非账号响应拒绝。

Cookie 解析还覆盖重复名称以后值覆盖前值。P0 最终 Hypium 套件总计 46 个用例，失败 0。

## DevEco 虚拟机验证

| API 24 检查项 | 结果 |
| --- | --- |
| 页面安装、启动与完整显示 | 通过 |
| 游客模式状态切换 | 通过 |
| Cookie 输入掩码、提交后清空 | 通过 |
| 空输入禁用、无效输入本地拒绝 | 通过 |
| 真实 Cookie 请求 `/api/v4/me` 返回有效账号 | 通过 |
| 校验成功后写入加密会话 | 通过 |
| 强制停止并重启应用后恢复登录态 | 通过 |
| 退出同时清理密文与 Asset Store 密钥 | 通过 |
| 清理后再次重启保持游客态 | 通过 |

复测只记录非敏感状态文案。UI 树未回显 Cookie，文档、日志和测试输出均未保存账号标识或 Cookie 值；真实会话在复测结束时已退出并清理。

## 当前结论

游客闭环和手动 Cookie 登录已经在 API 24 完成真实账号验证、加密保存、跨进程恢复与退出清理，`P0-LOGIN-01` 通过。安全保存仍只发生在知乎账号接口返回 HTTP 200 且账号字段完整之后。

二维码登录不是 P0 登录门禁的必选路径。`P0-SCAN-01` 已验证 Scan Kit 辅助扫码能力；本机生成二维码、轮询知乎登录状态和风控处理仍在 P1/MVP 阶段接入统一 Session Repository，不影响手动 Cookie MVP 的 P0 结论。
